<Window x:Class="DAY1.CategoryProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DAY1"
        mc:Ignorable="d"
        Title="Категории товаров" Height="263" Width="309">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

       
        <Label Grid.Row="0" 
               Content="Категория товаров" 
               HorizontalAlignment="Center" 
               FontWeight="SemiBold" 
               FontSize="14" 
               Margin="0,0,0,10"/>

        
        <ComboBox Grid.Row="1" 
                  x:Name="cbCategory" 
                  DisplayMemberPath="NAME_CATEGORY" 
                  HorizontalAlignment="Stretch" 
                  MaxWidth="260" 
                  Height="30"
                  SelectionChanged="ComboBox_SelectionChanged" RenderTransformOrigin="0.579,0.5"/>

     
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Bottom">

            <Button Width="50" Height="40"
                    Cursor="Hand" 
                    Click="VkButton_Click"
                    Background="Transparent" 
                    BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Image Source="/Resources/vk_icon.png" Stretch="Uniform"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Корзина" Click="Button_Click"/>

        </StackPanel>
        

    </Grid>
</Window>
