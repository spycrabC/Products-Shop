<Window x:Class="DAY1.ProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DAY1"
        mc:Ignorable="d"
        Title="Товары" Height="450" Width="800" MinWidth="600" MinHeight="350">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />

            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>

 
        <DataGrid x:Name="dgProducts" Grid.Row="0" AutoGenerateColumns="False" SelectionChanged="dgProducts_SelectionChanged" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Фото" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding PHOTO, TargetNullValue='pack://application:,,,/Resources/stub.png'}" 
                                   Width="60" Height="60" 
                                   Stretch="UniformToFill"/>
                            </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Название товара" Binding="{Binding NAME_PRODUCT}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Цена" Binding="{Binding PRICE}" Width="auto"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>


        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
 
                <ColumnDefinition Width="*" />

                <ColumnDefinition Width="Auto" />

            </Grid.ColumnDefinitions>


            <Button Grid.Column="0" Content="Назад" Width="100" Height="30" FontSize="14" Click="Button_Click_3"/>


            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="-" Width="40" Height="30" Click="Button_Click_1" FontSize="14" FontWeight="Bold"/>


                <Label x:Name="lblCount" Content="0" FontSize="18" FontWeight="Bold" Margin="15,0,15,0" VerticalAlignment="Center" HorizontalContentAlignment="Center" Width="40"/>

                <Button Content="+" Width="40" Height="30" IsEnabled="True" Click="Button_Click" FontSize="14" FontWeight="Bold"/>
            </StackPanel>


            <Button Grid.Column="2" x:Name="btnBasket" Content="В корзину" Width="130" Height="30" Click="Button_Click_2" FontSize="14" FontWeight="SemiBold"/>
        </Grid>
    </Grid>
</Window>
